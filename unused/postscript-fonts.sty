% 
% This package redefines the roman, sans serif, and typeface font families
% to use Postscript Type 1 fonts rather than the Computer Modern fonts used
% by default by LaTeX.  Although this redefinition tends to produce slightly
% less appealing document appearance, the document renders better when
% converted to PDF format (by ps2pdf(1) for example).  Since the Computer
% Modern fonts are converted to bitmapped Postscript Type 3 fonts when a
% document is converted to PDF, the resulting PDF file is rendered on the
% screen with ``blocky'' fonts (by acroread(1) for example).  However, when
% Type 1 fonts (which are built into the PDF specification) are used,
% acroread(1) applies antialiasing to the fonts, resulting in a much more
% pleasing on-screen appearance.
% 

\def\filename{postscript-fonts}
\def\fileversion{v1.0}
\def\filedate{1998/06/29}

\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{postscript-fonts}[\filedate\space\fileversion\space
Postscript Fonts]
\typeout{*** Postscript Fonts \fileversion\space <\filedate> (jef) ***}

\renewcommand{\sfdefault}{phv}
\renewcommand{\rmdefault}{ptm}
\renewcommand{\ttdefault}{pcr}

\endinput

